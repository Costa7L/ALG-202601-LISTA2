#include <stdio.h>
#include <stdlib.h>

int main(){

    int soma = 0;
    int i;

    for(i = 0; i <= 6; i++ ){
        soma += 4 + 1;

    }

    printf("A soma dos numeros 4 do domino e: %d\n", soma);

    
    system("PAUSE");

    return 0;
}